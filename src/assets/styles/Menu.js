import styled from 'styled-components'

const Menu = styled.ul`
    display: flex;
    gap: 25px;
    list-style-type: none;

    a {
        color: #fff;
        text-decoration: none;
    }
`

export default Menu